package com.impact.parser;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Smoke test: the Spring context starts. */
@SpringBootTest
class ParserApplicationTests {

    @Test
    void contextLoads() {
        // Intentionally empty: the assertion is that the context above started.
    }
}
